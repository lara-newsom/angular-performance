import { JlpButtonDirective } from './jlp-button.directive';

describe('JlpButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new JlpButtonDirective();
    expect(directive).toBeTruthy();
  });
});
