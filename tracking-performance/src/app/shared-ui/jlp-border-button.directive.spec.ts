import { JlpBorderButtonDirective } from './jlp-border-button.directive';

describe('JlpBorderButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new JlpBorderButtonDirective();
    expect(directive).toBeTruthy();
  });
});
